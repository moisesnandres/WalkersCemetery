%header
  .max-container.flex-container
    .logo-container
      = image_tag 'logo.png'
    %i.fa.fa-bars{"aria-hidden" => "true"}
    %nav
      %ul
        %li INICIO
        %li BÚSQUEDA
        %li OBITUARIO
        - if admin_signed_in?
          %li= link_to 'CERRAR SESIÓN', destroy_admin_session_path, method: :delete
